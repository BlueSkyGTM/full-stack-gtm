# Setup: Full-Stack GTM

Answer these questions once. Your answers configure the workspace permanently.

1. **Site URL** — The live course URL (e.g. https://learn.blueskygtm.engineer/)
   `{{SITE_URL}}`

2. **Repo URL** — The GitHub repo for the existing AI engineering curriculum
   `{{REPO_URL}}`

3. **Professor Synapse repo URL** — The repo to fork for Helix
   `{{SYNAPSE_REPO_URL}}`

4. **GLM API key** — Used for all Lyra/Newton/Hypatia/Echo invocations
   `{{GLM_API_KEY}}`

5. **Perplexity API key** — Used by Newton for GTM research
   `{{PERPLEXITY_API_KEY}}`

After answering, run: replace all `{{VARIABLE}}` placeholders across the workspace with your answers.
